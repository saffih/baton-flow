# Implementation Plan: The Task Lifecycle (HLD-004)

**Branch**: `018-task-lifecycle` | **Date**: 2026-06-03 | **Spec**: [spec.md](spec.md)

**Input**: `specs/018-task-lifecycle/spec.md` — HLD-004, constitution-source, HIGH risk

---

## Summary

Characterization spec for the four-state task lifecycle. `flow.py` already implements
every state, transition, and guard correctly — no source code changes. SCs 001–005 are
satisfied by 51 existing tests. This plan delivers one new test (SC-006: HLD-004 VERIFY
cited by ID) to complete contract coverage.

---

## Technical Context

**Language/Version**: Python 3.x (standard library only)

**Primary Dependencies**: pytest, `flow.py` (brownfield)

**Storage**: SQLite — state enforced by DB CHECK constraint; tests invoke via `flow.*` API only

**Testing**: pytest — all new tests added to `test_flow.py`; baseline is 51 green tests

**Target Platform**: Local CLI, macOS/Linux

**Project Type**: CLI tool — brownfield characterization only

**Constraints**:
- SC-007: all 51 prior tests remain green after every slice
- Principle IV: new test confirmed RED before implementation assertion
- No new source files; no schema changes; no flow.py edits

---

## Constitution Check

| Principle | Assessment |
|---|---|
| I — HLD is SoT | HLD-004 governs; spec aligns. ✓ |
| II — SQLite is SoT | No schema changes. Tests invoke via flow API only. ✓ |
| III — AI-Agnostic, contracts separated | No AI names. State machine is processing, not contract. ✓ |
| IV — TDD + Ratchet | New test confirmed RED first. NON-NEGOTIABLE. |
| V — Build order + simplicity | 001 → 009 → **004** is correct. One new test. No new abstractions. ✓ |

**Gate: PASS.**

---

## SC Coverage Map

| SC | Requirement | Existing coverage | Gap |
|---|---|---|---|
| SC-001 | All valid state transitions have a test | test_add_creates_pending, test_next_claims_in_progress, test_done_marks_done_with_outcome, test_escalate_blocks, test_reply_wakes_task, test_reopen_done_task | COVERED |
| SC-002 | done rejected when unfinished children exist | test_parent_cannot_be_done_with_open_children, test_done_rejected_while_escalated | COVERED |
| SC-003 | done is reopenable | test_reopen_done_task, test_reopen_rejects_non_done | COVERED |
| SC-004 | blocked wakes when dependencies resolve | test_parent_wakes_when_all_children_done, test_reply_wakes_task | COVERED |
| SC-005 | four-state invariant (no fifth state) | test_only_four_states, test_state_column_rejects_unknown_state | COVERED |
| SC-006 | HLD-004 VERIFY cited by ID in ≥1 test | test_only_four_states cites "HLD-004:" comment only; VERIFY text not cited | Need `test_hld004_verify_invariant` |
| SC-007 | All 51 prior tests remain green | Baseline confirmed | No gap |

---

## Project Structure

### Documentation (this feature)

```text
specs/018-task-lifecycle/
├── plan.md              ← this file
└── tasks.md             ← generated by /speckit-tasks
```

No research.md, data-model.md, or contracts/ needed — characterization only.

### Source Code (affected files only)

```text
test_flow.py             ← add test_hld004_verify_invariant (1 new test)
flow.py                  ← read-only; no changes
```

---

## Implementation Slice

### Slice A — Baseline

**Goal**: 51 prior tests green.

**Gate**: `pytest test_flow.py` exits 0 with exactly 51 passed.

### Slice B — SC-006: HLD-004 VERIFY invariant cited by ID

**Goal**: One new test cites "HLD-004 VERIFY" and the full invariant text by ID, and
asserts the structural guarantees of the VERIFY clause against the running system.

**Test name**: `test_hld004_verify_invariant`

**Mechanism**:
- Function docstring cites "HLD-004 VERIFY" and the full text:
  `"only four states exist; a task cannot be done with unfinished children; done is reopenable; blocked wakes to pending only when all dependencies resolve"`
- Assert `flow.STATES == ("pending", "in_progress", "blocked", "done")` (four states, exact order)
- Assert reopen is callable on a done task (reopenable)
- Assert done raises on a task with unfinished children (unfinished-children guard)

**TDD**: confirm `test_hld004_verify_invariant` absent → write → confirm GREEN → full suite green.

---

## Agent Context

<!-- SPECKIT START -->
Active feature plan: `specs/018-task-lifecycle/plan.md` (G03 — spec 004, plan phase complete)
<!-- SPECKIT END -->
