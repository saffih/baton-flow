# Implementation Plan: What It Is (HLD-001)

**Branch**: `016-what-it-is` | **Date**: 2026-06-03 | **Spec**: [spec.md](spec.md)

**Input**: HLD-001 — purpose, baton concept, three pains, runner contract

## Summary

Document what Baton Flow is and how a runner interacts with it, faithfully reflecting
HLD-001. Deliverables are updates to `README.md` and `core.md`. The key task is fixing
a live Principle III violation: README.md currently names specific AI systems
("Claude now, Devin/Codex later"), which the constitution forbids and the test suite
must guard against.

## Technical Context

**Language/Version**: Python 3.x (existing `flow.py`; no new code for this spec)

**Primary Dependencies**: None — documentation-only changes

**Storage**: N/A (no new schema; SQLite remains the single source of truth per Principle II)

**Testing**: pytest (`test_flow.py`); characterization baseline = 45 tests green

**Target Platform**: CLI tool on Linux/macOS

**Project Type**: CLI tool — documentation spec

**Performance Goals**: N/A

**Constraints**: Must not break any of the 45 existing tests; `names no AI` test must
pass on both `core.md` and `README.md` after changes.

**Scale/Scope**: Two files — `README.md` and `core.md`. No other files in scope.

## Constitution Check

| Principle | Status | Notes |
|-----------|--------|-------|
| I. HLD source of truth | ✅ PASS | All changes trace to HLD-001; no HLD contradiction |
| II. SQLite single source of truth | ✅ PASS | Documentation-only; no new state or markdown-as-input |
| III. AI-agnostic runner | ⚠ FIX IN SCOPE | README.md currently names Claude/Devin/Codex — plan fixes this |
| IV. Test-first | ✅ PASS | New test assertion for README.md AI-agnosticism written red before fix |
| V. Common foundation first | ✅ PASS | HLD-001 is build-order position 1 |

Gate: no unresolved violations. The Principle III breach is in current code; this plan
resolves it.

## Project Structure

### Documentation (this feature)

```text
specs/016-what-it-is/
├── plan.md              ← this file
├── research.md          ← gap analysis (done)
├── data-model.md        ← canonical vocabulary (done)
└── tasks.md             ← generated by /speckit-tasks
```

### Source files in scope

```text
README.md                ← fix AI names, add three-pains section, add stripped-scope note
core.md                  ← strengthen binary reply rule sentence
test_flow.py             ← extend AI-agnostic test to cover README.md
```

## Implementation Slices

### Slice A — Fix AI-agnostic violation in README.md (Principle III gate)

**Why first**: This is the only item that breaks a constitution rule and a test. Fix it
before any other documentation work so the test suite is green as the baseline for
subsequent changes.

**Changes**:
1. In `test_flow.py`: add an assertion that `README.md` does not contain any of
   `["Claude", "Devin", "Codex", "GPT", "Gemini"]`. Confirm red.
2. In `README.md`: replace `"an AI session — Claude now, Devin/Codex later — or you"`
   with `"an AI session, or you"`. Confirm test green.
3. Run full suite (45 tests + new assertion). All green.

**Gate**: `test_ai_agnostic_readme` passes; no prior test regresses.

---

### Slice B — Add three-pains section to README.md (FR-002)

**Changes**:
1. After the opening paragraph in README.md, add the explicit pain list from HLD-001:
   - Context loss between AI sessions on multi-step work.
   - No visibility into what the AI is doing while it works.
   - No way to steer without starting over.
2. Add a brief "The baton solves all three" closing sentence.
3. Run full suite. All green.

**Gate**: Full suite green. A reader can now answer SC-001 from README.md alone.

---

### Slice C — Add stripped-scope note to README.md (FR-007)

**Changes**:
1. Add a "What it is not" section (or a brief inline note) citing HLD-011's boundaries:
   no sockets, no web UI, no health daemons, no migration tooling — deliberately stripped.
2. Run full suite. All green.

**Gate**: Full suite green. SC-001 contributor test satisfied.

---

### Slice D — Strengthen binary reply rule in core.md (FR-005)

**Changes**:
1. In the "On a woken task" section of `core.md`, add one sentence before the bullet
   list explicitly stating the routing invariant:
   "A reply **about this task** appends to the baton and unblocks it; a reply about
   anything else becomes a new task — the original stays blocked."
2. Run full suite. All green.

**Gate**: Full suite green. FR-005 satisfied. The invariant matches the HLD-007 VERIFY
line verbatim.

---

## Dependency Order

```
Slice A (gate: AI-agnostic test green)
  └── Slice B (README.md gains three-pains section)
        └── Slice C (README.md gains stripped-scope note)
              └── Slice D (core.md binary reply rule explicit)
```

All slices are sequential. Slice A is a hard gate; B/C/D are safe to batch if all tests
stay green between each.

## Complexity Tracking

No violations to justify. Documentation-only changes.
