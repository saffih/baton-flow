# Anchor Implementation Status

Evidence-based reflection of where each HLD anchor stands (code + tests), using
HLDspec's `anchor_coverage` vocabulary. Generated during the HLDspec dogfood run.

| Anchor | Status | Evidence |
|---|---|---|
| HLD-003 Core model | implemented | enforced by tests (agnostic loop, SQLite-truth) |
| HLD-004 Lifecycle | implemented | states in `flow.py`, 4 test cites |
| HLD-005 Wait model | implemented | escalate/split→blocked, wake; tested |
| HLD-008 Baton | implemented | DB-truth + survives-markdown-deletion test |
| HLD-009 CLI contract | implemented | verb-set test |
| HLD-010 Work routing | **implemented (this slice)** | label column + `sessions` table; soft-affinity tests |
| HLD-013 Concurrency/durability | **implemented (this slice)** | WAL+busy_timeout+synchronous; atomic claim; one-tx-per-op; concurrent-claim test |
| HLD-006 Escalation triggers | partial — by design | `escalate()` exists; triggers are runner judgment (`core.md`), not mechanizable |
| HLD-007 Human-in-the-loop | partial — by design | `reply()` exists; the "about this task?" branch is runner judgment |
| HLD-014 Recovery | deferred | `STATUS: planned`; its own future slice once 013 is proven |
| HLD-001/002/011/012 | not applicable | purpose / vocabulary / out-of-scope / technology — no code artifact |

**Partial-by-design** is distinct from unfinished: HLD-006/007 are deliberately
runner judgment and must not be mistaken for missing implementation.

This reflection should eventually be generated by HLDspec itself (the planner has
no "already-implemented/locked" anchor state today) so a run can say, per anchor,
what is implemented vs partial vs deferred. Tracked as an HLDspec improvement.
